# [Infra #2] nginx reverse proxy (mesmo dominio).
# TODO: servir o build do front (dist) em "/"; proxy_pass de /api, /oauth2, /login -> backend:8080;
#       repassar headers Host e X-Forwarded-* (necessario pro redirect do OAuth no Spring).
# Ver issue: https://github.com/vitinh0z/portfolio/issues/2
